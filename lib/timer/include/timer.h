#ifndef TIMER_H_
#define TIMER_H_

/**
 * @brief Provide the current ticks in milliseconds
 * 
 * @return uint32_t returns the ticks in milliseconds
 */
uint32_t GetMillis(void);

#endif /* TIMER_H_ */
